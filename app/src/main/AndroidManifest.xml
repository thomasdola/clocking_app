<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eurekacachet.clocling">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".ClockingApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.view.login.LoginActivity" />
        <activity android:name=".ui.view.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.view.bio.BioActivity"
            android:launchMode="singleTask" />
        <activity android:name=".ui.view.clocking.ClockingActivity" />

        <service android:name=".utils.services.SocketService" />
        <service android:name=".utils.services.SyncService" />

        <receiver
            android:name=".utils.services.SyncService$SyncOnConnectionAvailable"
            android:enabled="false" />
        <receiver
            android:name=".utils.receivers.BootUpReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <!-- <receiver -->
        <!-- android:name=".utils.receivers.ShutDownReceiver" -->
        <!-- android:enabled="true" -->
        <!-- android:exported="false"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.REBOOT" /> -->
        <!-- <action android:name="android.intent.action.ACTION_SHUTDOWN" /> -->
        <!-- </intent-filter> -->
        <!-- </receiver> -->


        <!-- <service -->
        <!-- android:name=".utils.services.SocketOffService" -->
        <!-- android:enabled="true" -->
        <!-- android:exported="true"></service> -->
    </application>

</manifest>